---
name: drama-maker
description: "短剧制作智能体。端到端自动化短剧制作流水线：从创作主题 -> 故事文本 -> 剧本 -> 分镜脚本 -> 角色关键帧 -> 视频片段生成 -> 粗剪 -> 精剪迭代 -> 配乐字幕成片。当用户需要制作短剧、微短剧、竖屏短剧、短视频剧情时必须调用。核心能力：(1) 给定创作主题，自动生成完整故事文本；(2) 将故事转化为标准短剧剧本（场景/对白/动作指示）；(3) 拆分逐镜头分镜脚本并确定配乐基调；(4) 为每个角色生成关键帧设定图，建立角色卡保证一致性；(5) 根据分镜和角色卡逐镜头生成视频片段（图生图首帧 + 图生视频）；(6) 粗剪拼接生成粗版短剧；(7) 支持人工反馈精剪迭代，可回溯重新生成部分镜头；(8) 最终配以与剧情一致的音乐和字幕，生成完整短剧。触发词：做短剧、制作短剧、生成短剧、短剧制作、微短剧、竖屏短剧、短剧视频、拍短剧、创作短剧、短剧流水线"
version: "1.0.0"
---
# 短剧制作智能体 —— 端到端自动化短剧制作流水线

> 端到端 **短剧制作** 智能体：从**创作主题 → 故事 → 剧本 → 分镜 → 角色关键帧 → 视频生成 → 粗剪 → 精剪 → 配乐字幕成片**全流程自动编排。覆盖短剧制作全部场景。必须依赖 [drama-orchestrator SKILL](./skills/drama-orchestrator/SKILL.md)

---

## ⛔ 强制执行规则（MUST — 不可跳过、不可简化）

**本 Skill 被加载后，下一步动作必须是：Read `./skills/drama-orchestrator/SKILL.md` 并严格按其 Stage 0 流程执行。**

### 禁止行为（违反即视为 Bug）

1. **禁止跳过 orchestrator 直接调用 ImageGen / VideoGen / ffmpeg** — 无论任务多简单，都必须先走 orchestrator 的 Stage 0 入口判断、信用预算预估和用户确认
2. **禁止跳过角色卡直接文生视频** — S5 视频生成必须使用 S4 产出的角色卡 reference_image 作为图生图输入，角色一致性是核心质量保障
3. **禁止在 thinking 中自行判断"这个任务简单，不需要走流程"** — 简单/复杂由 orchestrator 内部的 Stage 0 决定
4. **禁止跳过信用预算确认** — ImageGen / VideoGen 消耗大量 credits，S0 完成后必须预估并等待用户确认才进入 S1

### 正确的执行顺序（唯一合法路径）

```
1. Read ./skills/drama-orchestrator/SKILL.md        ← 现在必须做这一步
2. 按 orchestrator Stage 0 识别入口类型 + 信用预算预估 + 用户确认
3. 按 Stage 链路由到对应 Agent/Skill
4. 由下游 Agent（story-agent / script-agent / ... / finalize-agent）执行具体任务
5. S6→S7 人工审看暂停，等待反馈后迭代
6. S8 完成 → present_files 展示最终成片
```

---

## 能力覆盖 —— 短剧制作全流程（9 个阶段）

| Stage | 能力 | 说明 |
|-------|------|------|
| S0 | 意图识别 + 编排 + 信用预算 | 识别用户意图，组合 Stage 链，预估 ImageGen/VideoGen 消耗并请求确认 |
| S1 | 故事文本生成 | 根据创作主题生成完整故事（人物/情节/结局），路由到领域 Expert |
| S2 | 剧本生成 | 将故事转化为标准短剧剧本（场景/对白/动作指示） |
| S3 | 分镜脚本 + 配乐基调 | 拆分逐镜头脚本（画面/运镜/时长/配乐基调），生成配乐计划 |
| S4 | 角色关键帧生成 | 为每个角色生成设定图（正面/侧面/表情集），建立角色卡 |
| S5 | 视频片段生成 | 逐镜头图生图首帧 + 图生视频，三层锁定保证角色一致性 |
| S6 | 粗剪 | 按分镜顺序拼接视频片段，加基础转场，生成粗版短剧 |
| S7 | 精剪（人工反馈迭代） | 根据人工反馈调整剪辑/重新生成不合格镜头/精修转场 |
| S8 | 配乐 + 字幕 + 成片 | 生成/获取配乐、生成字幕、音视频合成、最终烧录 |

---

**现在立即执行：Read `./skills/drama-orchestrator/SKILL.md`**
